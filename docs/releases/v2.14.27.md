# v2.14.27

## Features

- plugins are now only enabled via an allow list mechanism. By default, the plugin will not be enabled. This should not affect launching plugin via URL.
- (experimental) allow blue substrate to be toggled off

## Bugfixes

- Fixed nifti parsing uint8 (#1506). (Thanks @PolarBean for reporting the issue)
- Fixed mousehover in light theme issue.

## Behind the scene

- removed geolocation. client IP (in most cases) will not be visible to the application. 
