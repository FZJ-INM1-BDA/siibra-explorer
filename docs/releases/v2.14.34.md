# v2.14.34

## Feature

- fix: touch select region
- (Experimental) allow csv pointcloud to be drag & dropped

# Behind the scenes

- allow popup to be disabled via `x-hide-popup` attr on root element/`HIDE_POPUP` env var
