# v2.14.36

## Feature

- (experimental): render pointcloud KDE & points from geometry service
- allow overlay layer to be configured to show underlying segment
- region quick search prefers region name that case matches the search term
- region quick search will use single words, rather than whole term
